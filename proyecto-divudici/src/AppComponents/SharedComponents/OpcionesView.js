import React, { Component } from 'react'
import TituloOpciones from './TituloOpciones';
/*se tienen que importar los iconos para los botones que se veran */

export default class OpcionesView extends Component {
    

    render() {
        return (
            <div>
                <TituloOpciones titulo = "Opciones"/>
            </div>
        )
    }
}
